import { HighlightBlueDirective } from './highlight-blue.directive';

describe('HighlightBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBlueDirective();
    expect(directive).toBeTruthy();
  });
});
